{
    // ternary operator
    const name = user ? user.name : null;

    // nullish coalescing operator
    const name1 = user?.name ?? 'no name';


    // optional chaining
    const name2= user?.name;

}